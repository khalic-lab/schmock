{
  "name": "@schmock/openapi",
  "description": "OpenAPI/Swagger spec-driven auto-registration plugin for Schmock",
  "version": "1.12.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "bun build:lib && bun build:types",
    "build:lib": "bun build --minify --target browser --outdir=dist src/index.ts --external @apidevtools/swagger-parser --external @schmock/faker --external ajv --external @schmock/core",
    "build:types": "tsc -p tsconfig.json",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:bdd": "vitest run --config vitest.config.bdd.ts",
    "lint": "biome check src/*.ts",
    "lint:fix": "biome check --write --unsafe src/*.ts",
    "check:publish": "publint && attw --pack --ignore-rules cjs-resolves-to-esm"
  },
  "license": "MIT",
  "dependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@schmock/faker": "^1.12.0",
    "ajv": "^8.18.0"
  },
  "peerDependencies": {
    "@schmock/core": "^1.12.0"
  },
  "devDependencies": {
    "@amiceli/vitest-cucumber": "^6.3.0",
    "@types/json-schema": "^7.0.15",
    "@types/node": "^25.5.0",
    "openapi-types": "^12.1.3",
    "vitest": "^4.1.1"
  }
}
