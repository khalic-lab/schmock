{
  "name": "@schmock/angular",
  "version": "2.1.3",
  "description": "Angular HTTP interceptor adapter for Schmock",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun build:lib --watch",
    "clean": "rm -rf dist",
    "build": "bun run clean && bun run build:lib && bun run build:types",
    "build:lib": "bun build --minify src/index.ts --outdir dist --target browser --format esm --external @angular/common --external @angular/core --external @schmock/core --external @schmock/openapi --external rxjs",
    "build:types": "rm -f tsconfig.tsbuildinfo && tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:bdd": "vitest run --config vitest.config.bdd.ts",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "check:publish": "publint && attw --pack --ignore-rules cjs-resolves-to-esm"
  },
  "peerDependencies": {
    "@angular/common": ">=15.0.0 <22.0.0",
    "@angular/core": ">=15.0.0 <22.0.0",
    "@schmock/core": "^2.1.3",
    "@schmock/openapi": "^2.1.3",
    "rxjs": "^7.8.2"
  },
  "peerDependenciesMeta": {
    "@schmock/openapi": {
      "optional": true
    }
  },
  "devDependencies": {
    "@angular/common": "^21.2.13",
    "@angular/compiler": "^21.2.13",
    "@angular/core": "^21.2.13",
    "@angular/platform-browser": "^21.2.13",
    "@angular/platform-browser-dynamic": "^21.2.13",
    "@schmock/openapi": "^2.1.3",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7",
    "zone.js": "^0.16.2"
  }
}
