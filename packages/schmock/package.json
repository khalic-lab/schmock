{
  "name": "@schmock/schmock",
  "version": "2.0.0",
  "description": "All-in-one Schmock package — installs core, faker, validation, query, openapi, and CLI",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build:lib && bun build:types",
    "build:lib": "bun build --minify --target node --outdir=dist src/index.ts --external @schmock/core --external @schmock/faker --external @schmock/validation --external @schmock/query --external @schmock/openapi --external @schmock/cli",
    "build:types": "tsc -p tsconfig.json",
    "lint": "biome check src/",
    "lint:fix": "biome check --write --unsafe src/",
    "check:publish": "publint && attw --pack --ignore-rules cjs-resolves-to-esm"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  },
  "dependencies": {
    "@schmock/core": "^2.0.0",
    "@schmock/faker": "^2.0.0",
    "@schmock/validation": "^2.0.0",
    "@schmock/query": "^2.0.0",
    "@schmock/openapi": "^2.0.0",
    "@schmock/cli": "^2.0.0"
  },
  "keywords": [
    "mock",
    "api",
    "openapi",
    "swagger",
    "testing",
    "development"
  ],
  "author": "Khalic Lab",
  "license": "MIT"
}
