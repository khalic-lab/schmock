Feature: {{PLUGIN_TITLE}} Plugin
  As a developer
  I want to use the {{PLUGIN_NAME}} plugin
  So that I can {{PLUGIN_PURPOSE}}

  Scenario: Basic {{PLUGIN_NAME}} behavior
    Given I create a mock with the {{PLUGIN_NAME}} plugin
    When I request "GET /test"
    Then the response should reflect {{PLUGIN_NAME}} processing
